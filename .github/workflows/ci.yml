name: ci

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main
  workflow_call:
    inputs:
      full-run:
        description: >-
          Run every CI job unconditionally, ignoring path filters. Used by
          release.yml when calling this workflow, since a release build must
          validate everything regardless of which paths the tagged commit
          touched. The `inputs` context is empty (not this declared default)
          on a plain push/pull_request trigger, so normal path filtering
          applies unless a caller explicitly passes `full-run: true`.
        type: boolean
        default: false
        required: false

permissions:
  contents: read
  pull-requests: read

jobs:
  changes:
    name: detect changes
    runs-on: ubuntu-latest
    outputs:
      firmware: ${{ inputs.full-run == true || steps.filter.outputs.firmware == 'true' }}
      cli: ${{ inputs.full-run == true || steps.filter.outputs.cli == 'true' }}
      scripts: ${{ inputs.full-run == true || steps.filter.outputs.scripts == 'true' }}
      tools: ${{ inputs.full-run == true || steps.filter.outputs.tools == 'true' }}
      shared: ${{ inputs.full-run == true || steps.filter.outputs.shared == 'true' }}
    steps:
      - name: Checkout repository
        uses: actions/checkout@v6

      # Skipped (not errored) on a full-run call: dorny/paths-filter needs a
      # push/pull_request-shaped event to diff against, which a workflow_call
      # invocation doesn't provide. The job outputs above already force
      # 'true' for every category via inputs.full-run, so this step's result
      # is unused in that case.
      - name: Detect changed paths
        id: filter
        if: ${{ inputs.full-run != true }}
        uses: dorny/paths-filter@v3
        with:
          filters: |
            firmware:
              - 'firmware/**'
            cli:
              - 'cli/**'
            scripts:
              - 'scripts/**'
            tools:
              - 'tools/**'
            shared:
              - '.github/workflows/**'
              - 'cli/.goreleaser.yaml'
              - 'cliff.toml'
              - 'Justfile'

  firmware-format:
    name: firmware format
    needs: changes
    if: ${{ needs.changes.outputs.firmware == 'true' || needs.changes.outputs.shared == 'true' }}
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v6

      - name: Set up just
        uses: taiki-e/install-action@v2
        with:
          tool: just

      - name: Install Python tooling
        run: just setup

      - name: Run firmware format check
        run: just format-check

  firmware-build:
    name: firmware build
    needs: changes
    if: ${{ needs.changes.outputs.firmware == 'true' || needs.changes.outputs.shared == 'true' }}
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v6

      - name: Build firmware and log size reports
        uses: espressif/esp-idf-ci-action@v1
        with:
          esp_idf_version: v5.4
          target: esp32
          path: firmware
          command: idf.py build size size-components size-files

      - name: Upload firmware artifacts
        uses: actions/upload-artifact@v4
        with:
          name: firmware-build-esp32
          if-no-files-found: error
          path: |
            firmware/build/*.bin
            firmware/build/*.elf
            firmware/build/*.map
            firmware/build/flasher_args.json
            firmware/build/bootloader/bootloader.bin
            firmware/build/partition_table/partition-table.bin

  firmware-test:
    name: firmware host test
    needs: changes
    if: ${{ needs.changes.outputs.firmware == 'true' || needs.changes.outputs.scripts == 'true' || needs.changes.outputs.tools == 'true' || needs.changes.outputs.shared == 'true' }}
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v6

      - name: Run firmware host tests
        uses: espressif/esp-idf-ci-action@v1
        with:
          esp_idf_version: v5.4
          target: esp32
          path: firmware
          command: >-
            python -m pip install pytest requests pyserial &&
            cmake -S test -B test/build -DCMAKE_BUILD_TYPE=Debug
            -DENABLE_SANITIZERS=ON && cmake --build test/build &&
            cd test/build && ctest --output-on-failure &&
            cd ../.. &&
            python -m pytest -p no:cacheprovider
            test_integration/test_serial_bootloader_config.py

  cli-format:
    name: cli format
    needs: changes
    if: ${{ needs.changes.outputs.cli == 'true' || needs.changes.outputs.shared == 'true' }}
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v6

      - name: Set up Go
        uses: actions/setup-go@v6
        with:
          go-version-file: cli/go.mod
          cache-dependency-path: cli/go.sum

      - name: Set up just
        uses: taiki-e/install-action@v2
        with:
          tool: just

      - name: Run CLI format check
        run: just cli-fmt-check

  cli-lint:
    name: cli lint
    needs: changes
    if: ${{ needs.changes.outputs.cli == 'true' || needs.changes.outputs.shared == 'true' }}
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v6

      - name: Set up Go
        uses: actions/setup-go@v6
        with:
          go-version-file: cli/go.mod
          cache-dependency-path: cli/go.sum

      - name: Run golangci-lint
        uses: golangci/golangci-lint-action@v9
        with:
          version: v2.10
          working-directory: cli
          args: --timeout=5m

  cli-vet:
    name: cli vet
    needs: changes
    if: ${{ needs.changes.outputs.cli == 'true' || needs.changes.outputs.shared == 'true' }}
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v6

      - name: Set up Go
        uses: actions/setup-go@v6
        with:
          go-version-file: cli/go.mod
          cache-dependency-path: cli/go.sum

      - name: Set up just
        uses: taiki-e/install-action@v2
        with:
          tool: just

      - name: Run CLI vet
        run: just cli-vet

  cli-test:
    name: cli test
    needs: changes
    if: ${{ needs.changes.outputs.cli == 'true' || needs.changes.outputs.shared == 'true' }}
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v6

      - name: Set up Go
        uses: actions/setup-go@v6
        with:
          go-version-file: cli/go.mod
          cache-dependency-path: cli/go.sum

      - name: Run CLI tests
        working-directory: cli
        run: |-
          mapfile -t packages < <(go list ./... | grep -v '/test_e2e$')
          go test -race -coverprofile=coverage.out "${packages[@]}"

  cli-e2e:
    name: cli e2e
    needs: changes
    if: ${{ needs.changes.outputs.cli == 'true' || needs.changes.outputs.firmware == 'true' || needs.changes.outputs.shared == 'true' }}
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v6

      - name: Set up Go
        uses: actions/setup-go@v6
        with:
          go-version-file: cli/go.mod
          cache-dependency-path: cli/go.sum

      - name: Set up just
        uses: taiki-e/install-action@v2
        with:
          tool: just

      - name: Run CLI e2e tests
        run: just test-e2e

  cli-build:
    name: cli build
    needs: changes
    if: ${{ needs.changes.outputs.cli == 'true' || needs.changes.outputs.shared == 'true' }}
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v6

      - name: Set up Go
        uses: actions/setup-go@v6
        with:
          go-version-file: cli/go.mod
          cache-dependency-path: cli/go.sum

      - name: Build CLI
        working-directory: cli
        run: go build -o /dev/null .
